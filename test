} catch (IOException e) {
String url = props.getProperty("db.url");
if (out != null) {
out.write(”aString”);
String url = props.getProperty("db.url");
String driverClass = "oracle.jdbc.driver.OracleDriver";
} finally {
if (out != null) {
hello world
